/**
 * Stub de `server-only` para os testes.
 *
 * Em produção o Next resolve o pacote real, que impede o módulo de ser
 * incluído em um bundle de cliente. Sob o Node puro dos testes esse guarda
 * lançaria erro — aqui ele vira um no-op, sem afetar o build.
 */
export {};
